from controller import CONTROLLER, abstractmethod


class PIDCONTROLLER(CONTROLLER):

    def compute_control_signal(args):
        #return kp * error + kd * error_derivative + ki * error_integral
        pass